<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HSP Calculator</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 50px;
        }
        h1 {
            color: #333;
        }
        .container {
            max-width: 500px;
            margin: auto;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }
        input[type="text"] {
            width: 100%;
            padding: 8px;
            box-sizing: border-box;
        }
        button {
            padding: 10px 15px;
            background-color: #5cb85c;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background-color: #4cae4c;
        }
        .results {
            margin-top: 30px;
        }
        .error {
            color: red;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Hansen Solubility Parameter (HSP) Calculator</h1>
        <form method="POST">
            <div class="form-group">
                <label for="iupac_name">Enter IUPAC Name:</label>
                <input type="text" name="iupac_name" id="iupac_name" required>
            </div>
            <button type="submit">Calculate HSP Values</button>
        </form>

        {% if hsp_values %}
        <div class="results">
            <h2>HSP Values for {{ request.form['iupac_name'] }}</h2>
            <p><strong>δd:</strong> {{ hsp_values['δd'] }} MPa^(1/2)</p>
            <p><strong>δp:</strong> {{ hsp_values['δp'] }} MPa^(1/2)</p>
            <p><strong>δhb:</strong> {{ hsp_values['δhb'] }} MPa^(1/2)</p>
        </div>
        {% elif error %}
        <div class="error">
            <p>{{ error }}</p>
        </div>
        {% endif %}
    </div>
</body>
</html>
